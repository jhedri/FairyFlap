import Foundation

/// Accessibility identifiers shared between the app and UI tests.
enum AccessibilityID {
    static let homeTitle = "home_title"
    static let lastScore = "last_score"
    static let topScore = "top_score"
    static let playButton = "play_button"
    static let tapHint = "tap_hint"
    static let scoreLabel = "score_label"
}
